services:
  # chromadb:
  #   image: ghcr.io/chroma-core/chroma:latest
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - chroma_data:/chroma/chroma
  #   environment:
  #     - CHROMA_DB_IMPL=duckdb+parquet
  #     - PERSIST_DIRECTORY=/chroma/chroma

  viewer:
    build:
      context: ./viewer
    ports:
      - "3100:3000"
    environment:
      - CHROMA_API_ADDR=http://host.docker.internal:8000
    # volumes:
    #   - ./viewer:/app
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 512M